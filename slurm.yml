---
# ansible-playbook -i inventory slurm.yml

- hosts: compute-nodes
  pre_tasks:
    - setup:

- hosts: slurm-masters
  become: yes
  roles:
    - { role: slurm, is_controller: yes }

- hosts: compute-nodes
  become: yes
  roles:
    - { role: slurm, is_compute: yes }
